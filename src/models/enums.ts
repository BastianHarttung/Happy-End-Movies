export enum ECategory {
  MOVIE = "movie",
  TV = "tv",
  PERSON = "person",
  ALL_CATEGORIES = "allCategories",
  SEARCH_MULTI = "multi",
}

export enum EHappyEndFilter {
  ALL_ENDS = "allEnds",
  TRUE = "true",
  FALSE = "false"
}

export enum EHasHappyEnd {
  NEUTRAL = "neutral",
  TRUE = "true",
  FALSE = "false"
}

export enum EGender {
  NOT_SPECIFIED = 0,
  FEMALE = 1,
  MALE = 2,
  DIVERS = 3,
}
